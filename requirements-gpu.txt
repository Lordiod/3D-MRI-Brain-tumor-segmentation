# GPU Requirements for Training
# Use this file for setting up the training environment with GPU support

# Core ML framework with GPU support
tensorflow==2.10.0

# CUDA and cuDNN (install via conda)
# conda install cudatoolkit=11.2 cudnn=8.1.0 -c conda-forge

# Training-specific packages
jupyter>=1.0.0
notebook>=6.4.0
ipywidgets>=7.6.0

# Data processing for training
pandas>=1.3.0
h5py>=3.1.0
tqdm>=4.62.0

# Visualization for training monitoring
seaborn>=0.11.0
plotly>=5.0.0

# Model optimization
tensorboard>=2.8.0

# Memory optimization
psutil>=5.8.0

# Base requirements (same as main requirements.txt)
customtkinter>=5.2.0
numpy>=1.21.0
nibabel>=4.0.0
matplotlib>=3.5.0
opencv-python>=4.6.0
Pillow>=9.0.0
scikit-learn>=1.1.0

# Additional training utilities
albumentations>=1.3.0  # Data augmentation
keras-tuner>=1.1.0     # Hyperparameter tuning (optional)
wandb>=0.13.0          # Experiment tracking (optional)

# Note: This setup is optimized for:
# - TensorFlow 2.10.0
# - CUDA 11.2
# - cuDNN 8.1
